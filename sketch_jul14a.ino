/*
  ====================================================================
  スマートロックシステム構成に関するお知らせ (README)
  ====================================================================

  チームメンバーからの仕様変更により、このプロジェクトは
  「Arduino Uno」と「ESP32」の2台構成へ分割されました。
  Arduino IDEの仕様により、.inoファイルはそのファイル名と同名の
  フォルダ内に配置する必要があります。そのため、各プログラムは
  以下のサブフォルダへ移動しました。

  ▼ 書き込み手順：

  1. 【Arduino Uno】
     - 「ArduinoUno_Main」フォルダ内の「ArduinoUno_Main.ino」を開いてください。
     - Arduino IDEのボード設定で「Arduino Uno」を選択し、書き込みます。

  2. 【ESP32】
     - 「ESP32_WiFi」フォルダ内の「ESP32_WiFi.ino」を開いてください。
     - Arduino IDEのボード設定で「ESP32 Dev Module」などを選択し、書き込みます。

  ※ この「sketch_jul14a.ino」は現在使用されていません。
*/

void setup() {}
void loop() {}
